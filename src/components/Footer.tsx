const Footer = () => {
    return (
        <section className="flex flex-col gap-4 items-center pb-5">
            {/* Empty footer with no banners */}
        </section>
    );
};

export default Footer;